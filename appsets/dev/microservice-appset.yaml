apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: dev-appset
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - matrix:
      generators:
      - git:
          repoURL: https://github.com/schmhj/cluster-config.git
          revision: HEAD
          directories:
          - path: apps/*/values/envs/dev
      - git:
          repoURL: https://github.com/schmhj/cluster-config.git
          revision: HEAD
          files:
          - path: "{{.path.path}}/config.json"
  
  template:      
    metadata:
      # .path.path is the directory (from directories generator)
      # .appName is from config.json (from files generator)
      name: '{{index .path.segments 4}}-{{ .appName }}'
    spec:
      project: default
      sources:
        - repoURL: '{{ .chartRepo }}'
          path: '{{ .chartPath }}'
          targetRevision: '{{ .chartVersion }}'
          helm:
            valueFiles:
              - '{{ .path.path }}/values.yaml'
            
        - repoURL: https://github.com/schmhj/cluster-config.git
          targetRevision: HEAD
          path: '{{ .path.path }}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{index .path.segments 4}}-{{index .path.segments 1}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            maxDuration: 3m0s
            factor: 2