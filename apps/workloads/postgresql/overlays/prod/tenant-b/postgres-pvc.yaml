apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: workloads
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: oci-bv
  resources:
    requests:
      storage: 50Gi