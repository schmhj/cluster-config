apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-data
  namespace: workloads
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: oci-bv
  resources:
    requests:
      storage: 50Gi